# Contributing

Open an issue before large changes. Keep new behavior covered by focused tests and, when it affects a metric, add or update a reproducible benchmark.

```bash
python -m pip install -e .[dev]
pytest -q
```

Keep pull requests narrow enough to review in one sitting.
